{
  "name": "@soundsystem/network",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf tsconfig.tsbuildinfo ./dist",
    "build": "yarn run prebuild && tsc --build",
    "test": "jest --detectOpenHandles --forceExit --runInBand"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "@types/node": "^14.14.16",
    "@types/uuid": "^8.3.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "uuid": "^8.3.2",
    "@soundsystem/common": "1.0.0"
  }
}
